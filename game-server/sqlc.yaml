version: "2"
sql:
  - engine: postgresql
    schema: "db/migrations"
    queries: "db/queries"
    gen:
      go:
        package: "q"
        out: "internal/db/q"
        emit_interface: true
        emit_json_tags: true
        query_parameter_limit: 0
        sql_package: "pgx/v5"
